# cid-workflow-version: 0.5.0

# This file is generated by the CID Workflow GitLab App.
# DO NOT EDIT!

# global defaults
default:
  image: quay.io/podman/stable@sha256:866bbcac26adb7ff238145405fa0ade33bd7e9c7bb77a5c05f819dce8a753063 # v5.5.2-immutable
  before_script:
    - bash .cid/scripts/install.sh "0.6.0" ""

# variables
variables:
  # git settings
  GIT_STRATEGY: clone
  GIT_DEPTH: 10

# stages
stages:
    - build
    - lint
    - scan
    - publish

# configuration snippets
.default-job: &default_job
  
  tags:
    - saas-linux-small-amd64
  allow_failure: false
  interruptible: true
  retry:
    max: 2
    when:
      - runner_system_failure
      - api_failure
      - scheduler_failure

# Nightly
## helm-build [chart-library]
"nightly/helm-build [chart-library]":
    <<: *default_job
    stage: build
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "schedule" && $CI_PIPELINE_SCHEDULE_DESCRIPTION == "nightly"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "nightly" --step "helm-build-chart-library"
    artifacts:
      name: "nightly/helm-build [chart-library]"
      paths:
        - .dist/helm-build-chart-library/
      expire_in: 1 day
## helm-build [keycloak]
"nightly/helm-build [keycloak]":
    <<: *default_job
    stage: build
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "schedule" && $CI_PIPELINE_SCHEDULE_DESCRIPTION == "nightly"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "nightly" --step "helm-build-keycloak"
    artifacts:
      name: "nightly/helm-build [keycloak]"
      paths:
        - .dist/helm-build-keycloak/
      expire_in: 1 day
## helm-lint [chart-library]
"nightly/helm-lint [chart-library]":
    <<: *default_job
    stage: lint
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "schedule" && $CI_PIPELINE_SCHEDULE_DESCRIPTION == "nightly"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "nightly" --step "helm-lint-chart-library"
    artifacts:
      name: "nightly/helm-lint [chart-library]"
      paths:
        - .dist/helm-lint-chart-library/
      expire_in: 1 day
## helm-lint [keycloak]
"nightly/helm-lint [keycloak]":
    <<: *default_job
    stage: lint
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "schedule" && $CI_PIPELINE_SCHEDULE_DESCRIPTION == "nightly"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "nightly" --step "helm-lint-keycloak"
    artifacts:
      name: "nightly/helm-lint [keycloak]"
      paths:
        - .dist/helm-lint-keycloak/
      expire_in: 1 day
## gitleaks-scan
"nightly/gitleaks-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "schedule" && $CI_PIPELINE_SCHEDULE_DESCRIPTION == "nightly"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "nightly" --step "gitleaks-scan"
    artifacts:
      name: "nightly/gitleaks-scan"
      paths:
        - .dist/gitleaks-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/gitleaks-scan/report/gl-codequality/*.json
## semgrep-scan
"nightly/semgrep-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "schedule" && $CI_PIPELINE_SCHEDULE_DESCRIPTION == "nightly"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "nightly" --step "semgrep-scan"
    artifacts:
      name: "nightly/semgrep-scan"
      paths:
        - .dist/semgrep-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/semgrep-scan/report/gl-codequality/*.json
## trivyfs-scan
"nightly/trivyfs-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "schedule" && $CI_PIPELINE_SCHEDULE_DESCRIPTION == "nightly"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "nightly" --step "trivyfs-scan"
    artifacts:
      name: "nightly/trivyfs-scan"
      paths:
        - .dist/trivyfs-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/trivyfs-scan/report/gl-codequality/*.json

# Pull Request
## helm-build [chart-library]
"pull-request/helm-build [chart-library]":
    <<: *default_job
    stage: build
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
        changes:
          paths:
            - charts/chart-library/**/*
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "pull-request" --step "helm-build-chart-library"
    artifacts:
      name: "pull-request/helm-build [chart-library]"
      paths:
        - .dist/helm-build-chart-library/
      expire_in: 1 day
## helm-build [keycloak]
"pull-request/helm-build [keycloak]":
    <<: *default_job
    stage: build
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
        changes:
          paths:
            - charts/keycloak/**/*
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "pull-request" --step "helm-build-keycloak"
    artifacts:
      name: "pull-request/helm-build [keycloak]"
      paths:
        - .dist/helm-build-keycloak/
      expire_in: 1 day
## helm-lint [chart-library]
"pull-request/helm-lint [chart-library]":
    <<: *default_job
    stage: lint
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
        changes:
          paths:
            - charts/chart-library/**/*
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "pull-request" --step "helm-lint-chart-library"
    artifacts:
      name: "pull-request/helm-lint [chart-library]"
      paths:
        - .dist/helm-lint-chart-library/
      expire_in: 1 day
## helm-lint [keycloak]
"pull-request/helm-lint [keycloak]":
    <<: *default_job
    stage: lint
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
        changes:
          paths:
            - charts/keycloak/**/*
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "pull-request" --step "helm-lint-keycloak"
    artifacts:
      name: "pull-request/helm-lint [keycloak]"
      paths:
        - .dist/helm-lint-keycloak/
      expire_in: 1 day
## gitleaks-scan
"pull-request/gitleaks-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "pull-request" --step "gitleaks-scan"
    artifacts:
      name: "pull-request/gitleaks-scan"
      paths:
        - .dist/gitleaks-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/gitleaks-scan/report/gl-codequality/*.json
## semgrep-scan
"pull-request/semgrep-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "pull-request" --step "semgrep-scan"
    artifacts:
      name: "pull-request/semgrep-scan"
      paths:
        - .dist/semgrep-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/semgrep-scan/report/gl-codequality/*.json
## trivyfs-scan
"pull-request/trivyfs-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "pull-request" --step "trivyfs-scan"
    artifacts:
      name: "pull-request/trivyfs-scan"
      paths:
        - .dist/trivyfs-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/trivyfs-scan/report/gl-codequality/*.json

# Release
## helm-build [chart-library]
"release/helm-build [chart-library]":
    <<: *default_job
    stage: build
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_TAG =~ /^v[0-9]+.[0-9]+.[0-9]+$/'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "release" --step "helm-build-chart-library"
    artifacts:
      name: "release/helm-build [chart-library]"
      paths:
        - .dist/helm-build-chart-library/
      expire_in: 1 day
## helm-build [keycloak]
"release/helm-build [keycloak]":
    <<: *default_job
    stage: build
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_TAG =~ /^v[0-9]+.[0-9]+.[0-9]+$/'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "release" --step "helm-build-keycloak"
    artifacts:
      name: "release/helm-build [keycloak]"
      paths:
        - .dist/helm-build-keycloak/
      expire_in: 1 day
## gitleaks-scan
"release/gitleaks-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_TAG =~ /^v[0-9]+.[0-9]+.[0-9]+$/'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "release" --step "gitleaks-scan"
    artifacts:
      name: "release/gitleaks-scan"
      paths:
        - .dist/gitleaks-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/gitleaks-scan/report/gl-codequality/*.json
## semgrep-scan
"release/semgrep-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_TAG =~ /^v[0-9]+.[0-9]+.[0-9]+$/'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "release" --step "semgrep-scan"
    artifacts:
      name: "release/semgrep-scan"
      paths:
        - .dist/semgrep-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/semgrep-scan/report/gl-codequality/*.json
## trivyfs-scan
"release/trivyfs-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_TAG =~ /^v[0-9]+.[0-9]+.[0-9]+$/'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "release" --step "trivyfs-scan"
    artifacts:
      name: "release/trivyfs-scan"
      paths:
        - .dist/trivyfs-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/trivyfs-scan/report/gl-codequality/*.json
## changelog-generate
"release/changelog-generate":
    <<: *default_job
    stage: publish
    needs: ["release/gitleaks-scan", "release/helm-build [chart-library]", "release/helm-build [keycloak]", "release/semgrep-scan", "release/trivyfs-scan"]
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_TAG =~ /^v[0-9]+.[0-9]+.[0-9]+$/'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "release" --step "changelog-generate"
    artifacts:
      name: "release/changelog-generate"
      paths:
        - .dist/changelog-generate/
      expire_in: 1 day
## gitlab-release-publish
"release/gitlab-release-publish":
    <<: *default_job
    stage: publish
    needs: ["release/changelog-generate", "release/gitleaks-scan", "release/helm-build [chart-library]", "release/helm-build [keycloak]", "release/semgrep-scan", "release/trivyfs-scan"]
    dependencies: ["release/changelog-generate"]
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_TAG =~ /^v[0-9]+.[0-9]+.[0-9]+$/'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "release" --step "gitlab-release-publish"
    artifacts:
      name: "release/gitlab-release-publish"
      paths:
        - .dist/gitlab-release-publish/
      expire_in: 1 day

# Main
## helm-build [chart-library]
"main/helm-build [chart-library]":
    <<: *default_job
    stage: build
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "main"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "main" --step "helm-build-chart-library"
    artifacts:
      name: "main/helm-build [chart-library]"
      paths:
        - .dist/helm-build-chart-library/
      expire_in: 1 day
## helm-build [keycloak]
"main/helm-build [keycloak]":
    <<: *default_job
    stage: build
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "main"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "main" --step "helm-build-keycloak"
    artifacts:
      name: "main/helm-build [keycloak]"
      paths:
        - .dist/helm-build-keycloak/
      expire_in: 1 day
## helm-lint [chart-library]
"main/helm-lint [chart-library]":
    <<: *default_job
    stage: lint
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "main"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "main" --step "helm-lint-chart-library"
    artifacts:
      name: "main/helm-lint [chart-library]"
      paths:
        - .dist/helm-lint-chart-library/
      expire_in: 1 day
## helm-lint [keycloak]
"main/helm-lint [keycloak]":
    <<: *default_job
    stage: lint
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "main"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "main" --step "helm-lint-keycloak"
    artifacts:
      name: "main/helm-lint [keycloak]"
      paths:
        - .dist/helm-lint-keycloak/
      expire_in: 1 day
## gitleaks-scan
"main/gitleaks-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "main"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "main" --step "gitleaks-scan"
    artifacts:
      name: "main/gitleaks-scan"
      paths:
        - .dist/gitleaks-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/gitleaks-scan/report/gl-codequality/*.json
## semgrep-scan
"main/semgrep-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "main"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "main" --step "semgrep-scan"
    artifacts:
      name: "main/semgrep-scan"
      paths:
        - .dist/semgrep-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/semgrep-scan/report/gl-codequality/*.json
## trivyfs-scan
"main/trivyfs-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "main"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "main" --step "trivyfs-scan"
    artifacts:
      name: "main/trivyfs-scan"
      paths:
        - .dist/trivyfs-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/trivyfs-scan/report/gl-codequality/*.json
